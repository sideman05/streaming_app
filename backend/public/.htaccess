RewriteEngine On

# Let existing files/directories pass through
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route all API paths to front controller
RewriteRule ^ index.php [QSA,L]
